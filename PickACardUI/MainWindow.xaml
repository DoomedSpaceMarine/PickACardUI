<Window x:Class="PickACardUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PickACardUI"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>


        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="20">
            <Label x:Name="label" Content="How many cards should I pick?" HorizontalAlignment="Left" VerticalAlignment="Top" FontSize="20" Width="271"/>
            <ComboBox x:Name="comboBox" HorizontalAlignment="Left" VerticalAlignment="Top" Width="271" IsEditable="True"/>
        </StackPanel>
        <Button x:Name="Button" Content="Pick some cards" HorizontalAlignment="Center" Grid.Row="1" VerticalAlignment="Top" Margin="0,10,0,0"/>
        <ListBox x:Name="listBox" Margin="10,109,10,108" Grid.RowSpan="2" Grid.Column="1"/>

    </Grid>
</Window>
